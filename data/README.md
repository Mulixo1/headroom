# data/

This folder is intentionally empty in git.

Runtime config is written to the user config directory:

- macOS: `~/Library/Application Support/Headroom/`

See `config.example.json` for the schema.
